package stringcaculator

class Calculator() {
    val ZERO = 0

    fun add(text: String?): Int {
        var result = ZERO
        if (text.isNullOrEmpty()) {
            return result
        }
        return result
    }
}